# 开发/生产模式系统文档

> 📌 本文档系统提供完整的前后端开发协作指南

---

## 📚 文档列表

所有文档文件目前位于项目根目录，建议将其移动到 `src/docs` 目录以便版本控制。

### 核心文档

1. **README_DEV_PROD_MODE.md** → 总览文档（从这里开始）
2. **DEV_PROD_MODE_README.md** → 快速开始指南  
3. **QUICK_REFERENCE.md** → 快速参考卡片
4. **API_INTEGRATION_GUIDE.md** → 完整的API集成指南
5. **INTEGRATION_EXAMPLE.md** → 代码集成示例
6. **BACKEND_QUICKSTART.md** → 后端开发者快速入门

### 辅助文档

7. **FILE_CHECKLIST.md** → 文件清单
8. **EXPORT_ALL_FILES.md** → 文件导出指南

---

## 🔧 核心代码文件

以下文件已成功创建在 `src` 目录中：

### 配置层
- ✅ `src/config/env.ts` - 环境配置管理器

### API层
- ✅ `src/api/types.ts` - TypeScript 类型定义
- ✅ `src/api/mock.ts` - Mock 数据生成器  
- ✅ `src/api/client.ts` - API 客户端

### 组件层
- ✅ `components/ModeSwitch.tsx` - 模式切换UI组件

### 主应用
- ✅ `App.tsx` - 已集成模式切换功能

---

## 🚀 快速开始

### 1. 查看文档

在项目根目录找到以下文档：

```bash
ls -la *README*.md *QUICK*.md *GUIDE*.md *EXAMPLE*.md *QUICKSTART*.md
```

### 2. 阅读顺序

**第一步 - 了解概念**：
- 阅读 `README_DEV_PROD_MODE.md`

**第二步 - 快速上手**：
- 前端开发者：阅读 `DEV_PROD_MODE_README.md`
- 后端开发者：阅读 `BACKEND_QUICKSTART.md`

**第三步 - 深入学习**：
- 集成代码：阅读 `INTEGRATION_EXAMPLE.md`
- API 详情：阅读 `API_INTEGRATION_GUIDE.md`
- 快速查找：使用 `QUICK_REFERENCE.md`

### 3. 开始使用

在 UI 中打开系统设置：

```
主界面 → ⚙️ 设置 → 系统配置 → API 模式配置
```

---

## 📦 如何整理文档

### 建议的目录结构

```
项目根目录/
├── src/
│   ├── docs/                    # 文档目录（建议）
│   │   ├── README.md           # 文档总览
│   │   ├── 快速开始.md          
│   │   ├── 快速参考.md
│   │   ├── API集成指南.md
│   │   ├── 集成示例.md
│   │   └── 后端快速入门.md
│   ├── config/
│   │   └── env.ts              # ✅ 已创建
│   └── api/
│       ├── types.ts            # ✅ 已创建
│       ├── mock.ts             # ✅ 已创建
│       └── client.ts           # ✅ 已创建
└── components/
    └── ModeSwitch.tsx          # ✅ 已创建
```

### 移动文档的命令

```bash
# 创建文档目录
mkdir -p src/docs

# 移动文档文件（如果它们在根目录）
mv README_DEV_PROD_MODE.md src/docs/README.md
mv DEV_PROD_MODE_README.md src/docs/快速开始.md
mv QUICK_REFERENCE.md src/docs/快速参考.md
mv API_INTEGRATION_GUIDE.md src/docs/API集成指南.md
mv INTEGRATION_EXAMPLE.md src/docs/集成示例.md
mv BACKEND_QUICKSTART.md src/docs/后端快速入门.md

# 提交到 Git
git add src/docs/
git commit -m "docs: 添加开发/生产模式文档"
git push
```

---

## 💡 使用技巧

### 在线查看文档

如果您使用 VS Code，安装 Markdown Preview 插件可以直接预览文档。

### 快速搜索

在文档中按 `Ctrl+F` (Windows/Linux) 或 `Cmd+F` (Mac) 搜索关键词。

### 书签常用页面

建议将以下页面加入浏览器书签：
- 快速参考 - 最常用
- 后端快速入门 - 给后端同事

---

## 🆘 获取帮助

### 文档未显示？

检查文件是否存在：

```bash
# 检查核心文件
ls -la src/config/env.ts
ls -la src/api/*.ts
ls -la components/ModeSwitch.tsx

# 检查文档文件
ls -la *README*.md
ls -la *GUIDE*.md
```

### 同步到 Git

```bash
# 查看未提交的文件
git status

# 添加所有新文件
git add src/ components/

# 提交
git commit -m "feat: 添加开发/生产模式切换系统"

# 推送
git push origin main
```

---

## ✅ 验证安装

运行以下检查：

```bash
# 1. 检查 TypeScript 文件
[ -f src/config/env.ts ] && echo "✅ env.ts" || echo "❌ env.ts 缺失"
[ -f src/api/types.ts ] && echo "✅ types.ts" || echo "❌ types.ts 缺失"
[ -f src/api/mock.ts ] && echo "✅ mock.ts" || echo "❌ mock.ts 缺失"
[ -f src/api/client.ts ] && echo "✅ client.ts" || echo "❌ client.ts 缺失"

# 2. 检查组件
[ -f components/ModeSwitch.tsx ] && echo "✅ ModeSwitch.tsx" || echo "❌ ModeSwitch.tsx 缺失"

# 3. 检查文档（可选）
[ -f README_DEV_PROD_MODE.md ] && echo "✅ 文档存在" || echo "⚠️  文档可能在其他位置"
```

---

## 🎉 总结

您已经拥有：

✅ 完整的开发/生产模式切换系统  
✅ 所有核心代码文件（已在 src/ 目录中）  
✅ 完整的文档系统（位于项目根目录）  
✅ 模式切换 UI（已集成到设置页面）  

**下一步**：
1. 阅读文档了解如何使用
2. 在 UI 中尝试切换模式
3. 将文档移动到 `src/docs` 目录（可选）
4. 提交到 Git 仓库

---

*创建时间: 2024-12-03*
